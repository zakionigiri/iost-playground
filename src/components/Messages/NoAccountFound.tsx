import React from 'react'
import { Typography } from '@material-ui/core'

const NoAccountFound = () => {
  return <Typography>No account found on the blockchain</Typography>
}

export default NoAccountFound
